[project]
name = "querypy"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]
